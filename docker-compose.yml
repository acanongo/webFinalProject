version: '3'

services:
  web:
    build: .
    volumes:
       - .:/code

    ports:
      - 80:8000

    links:
      - redis:redis

    expose:
      - 8000

    command: python /code/realTimeChatCrypto/manage.py runserver 0.0.0.0:8000


  redis:
    image: redis:latest
    expose:
      - 6379
